pipeline {
    agent any

    tools {
        maven 'Default Maven'
    }

    stages {
        stage("Build") {
            steps {
                sh "mvn -version"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
